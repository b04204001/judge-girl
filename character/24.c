#include <stdio.h>
#include <string.h>

int main(){
    char input[100];
    int input_size;
    int vowel=0,;
    scanf("%s",input);
    input_size = strlen(input);
    for(int i=0 ; i < input_size ; i++){
        if(atoi(input[i])){

        }
        if(input[i] == 'a' || input[i] =='A'){

        }
        else{

        }
    }
    return 0;
}