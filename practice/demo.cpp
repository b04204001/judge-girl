#include <iostream>
#include<vector>
#include<string>
using namespace std;
int main () {

    vector<string> vec;
    string str;
    int t=2;
    while(t--) {
        cin>>str;
    vec.push_back(str);
    } 
    for(auto i:vec){
        if(i[0] == )
        cout<<i<<endl;
    }
    return 0;
}